apiVersion: apps/v1
kind: Deployment
metadata:
  name: delta-frontend-preprod
  namespace: delta
spec:
  replicas: 1
  selector:
    matchLabels:
      app: delta-frontend-preprod
  template:
    metadata:
      labels:
        app: delta-frontend-preprod
    spec:
      containers:
        - name: delta-frontend-preprod
          image: ghcr.io/mhd-sdk/delta/frontend:latest 
          imagePullPolicy: Always
          ports:
          - containerPort: 5173
      restartPolicy: Always
      tolerations:
        - key: "node-role.kubernetes.io/control-plane"
          operator: "Exists"
          effect: "NoSchedule"
      imagePullSecrets:
        - name: ghcr-secret